[package]
name = "scip-jack"
version = "0.1.0"
edition = "2024"
description = "A mathematically optimal Steiner tree solver in Rust, based on the directed cut formulation"
license = "MIT"
authors = ["warflash"]

[dependencies]
highs = "2.3"

[package.metadata.build]
# On Windows with VS 2026+, set CMAKE_GENERATOR=Ninja before building:
# $env:CMAKE_GENERATOR="Ninja"; cargo build
